import { AutoRouterOptions, AutoRouterType, IRequest } from './types';
export declare const AutoRouter: <RequestType extends IRequest = IRequest, Args extends any[] = any[], ResponseType = any>({ format, missing, finally: f, before, ...options }?: AutoRouterOptions<RequestType, Args>) => AutoRouterType<RequestType, Args, ResponseType>;
