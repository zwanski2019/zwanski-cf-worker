import { IRequest, RouterOptions, RouterType } from './types';
export declare const Router: <RequestType = IRequest, Args extends any[] = any[], ResponseType = any>({ base, routes, ...other }?: RouterOptions<RequestType, Args>) => RouterType<RequestType, Args, ResponseType>;
